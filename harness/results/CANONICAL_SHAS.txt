# Canonical sha256 prefixes for the selfgraph paper artifacts.
# Sourced by harness/reproduce.sh; mutating any value here means the
# next cold-run reproduction will be checked against this record.
#
# Each value is the first 16 hex characters of sha256(file).
#
# Generated on the deterministic floor (no LLM augmentation, sorted
# os.walk + pkgutil.walk_packages + set iteration). See REPRODUCE.md.
#
# corpus.literal — SELFGRAPH_OBJECTTYPE_MATCH=literal (BEFORE).
# corpus.relaxed — SELFGRAPH_OBJECTTYPE_MATCH=relaxed (AFTER).
# Both are first-class reproducible; reproduce.sh runs and checks
# both on every cold run.

EXPECTED_CORPUS_LITERAL_SHA=57a86e94ba5e211d
EXPECTED_CORPUS_RELAXED_SHA=3277086cf459e945
EXPECTED_ADVERSARIAL_SHA=09b408bd369dc89d
# rollback widened (post n=5): every guardrail-validated proposal in
# the relaxed corpus pipeline (n=72, includes all 9 bind_behavior
# proposals). Each promotion runs in a Runtime.fork so the wider
# sample neither contaminates other promotions nor mutates the main
# pipeline graph; corpus shas are independent of this run.
EXPECTED_ROLLBACK_SHA=ff7353d410ea7379

# future_event — the bind_behavior binding is causally LIVE under
# promotion and not under control. See harness/run_future_event.py
# for the binding-executor design note. Three trials, one per
# diligence-pack behavior selfgraph's proposer bound in the relaxed
# corpus run.
EXPECTED_FUTURE_EVENT_SHA=8418183932468a18

# extractor_recall — denominator counted by ast.walk over the
# installed activegraph package; numerator is the runtime-derived
# subset of nodes the existing extractor produces under each
# SELFGRAPH_OBJECTTYPE_MATCH mode. Measurement only — the extractor
# is not modified.
EXPECTED_EXTRACTOR_RECALL_SHA=82a971df7a9ad03c
